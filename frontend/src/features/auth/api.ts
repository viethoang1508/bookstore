/** Auth feature — call api_gateway auth routes via `src/lib/api-client`. Not wired in this boilerplate. */

export const authApi = {
  // login: (body: LoginRequest) => apiRequest({ method: "POST", path: "/auth/login", body }),
} as const;
